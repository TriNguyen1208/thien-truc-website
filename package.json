{
  "scripts": {
    "install": "concurrently \"cd client & npm install\" \"cd admin & npm install\" \"cd server & npm install\"",
    "start": "vite",
    "dev:client": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "dev:admin": "concurrently \"npm run dev --prefix admin\" \"npm run dev --prefix server\"",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix admin\" \"npm run dev --prefix server\"",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
